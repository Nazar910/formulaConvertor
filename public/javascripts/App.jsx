const React = require('react');
const Editor = require('./Editor.jsx');


const App = React.createClass({
    render(){
        return (
            <Editor/>
        )
    }
});

module.exports = App;
